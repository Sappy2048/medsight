version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: medsight-qdrant
    ports:
      - "6333:6333"  # HTTP API (Used by your Python scripts)
      - "6334:6334"  # gRPC API
    volumes:
      - ./data/qdrant_storage:/qdrant/storage
    restart: unless-stopped